"""Design a backend system that allows users to upload large files (up to 5GB).Design a backend system that allows users to upload large files (up to 5GB).
Once uploaded, the files should be processed asynchronously (e.g., extract metadata, scan for viruses, generate thumbnails) and results stored in a database."""